import 'package:flutter/material.dart';

class PatientContactScreen extends StatelessWidget {
  const PatientContactScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
